package leveldb4j.ext.java;

import java.util.Collection;

public final class CollectionsUtil {
    public static <T> void resize(Collection<T> collection, int size, T value) {
        if (collection == null)
            return;
        
        if (collection.size() < size) {
            collection.addAll
        }
    }
}
